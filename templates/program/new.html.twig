{% extends 'base.html.twig' %}

{% block title %}Ajouter une série{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center mt-3">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-secondary mt-3" type="submit">Ajouter</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
