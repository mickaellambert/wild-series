{% extends 'base.html.twig' %}

{% block title %}{{ episode.season.program.title }} - Saison {{ episode.season.number }} - Episode {{ episode.number }} {% endblock %}

{% block body %}
    <div class="media">
        <div class="media-body">
            <h1 class="mt-0">{{ episode.season.program.title }} - Saison {{ episode.season.number }} ({{ episode.season.year }}) - Episode {{ episode.number }}</h1>
            <p>{{ episode.synopsis }}</p>
        </div>
    </div>

    <a href="{{ path('program_index') }}">
        Retour à la liste des programmes
    </a>
{% endblock %}